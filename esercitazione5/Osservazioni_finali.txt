1. Nonostante quick_sort abbia complessità O(n log n) migliore rispetto a O(n²) di insertion_sort, per dimensioni piccole insertion_sort risulta più veloce in pratica. Questa soglia non è fissa, dipende dall'hardware e dai valori dei vettori casuali.
2. std::sort è sempre tra gli algoritmi più veloci.
3. La modalità Release stampa i risultati più velocemente rispetto alla modalità di Debug.